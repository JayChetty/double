const moves = {
  1: num => num + 1,
  2: num => num - 1,
  3: num => num * 2
};

function allCombs() {
  const out = [];
  [(1, 2, 3, 4, 5)].forEach(num => {
    [1, 2, 3].forEach(later => {
      out.push();
    });
  });
}

function findMin(number) {
  return;
}

const number = 9;

console.log(findMin(number));
